improve error handling

revise manual

update INSTALL instructions

alter test/Makefile to separate out tests that do not work

alter stderr output so that errors in processing produce ? instead of *

